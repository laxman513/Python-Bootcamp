# Program 08 — Selecting Rows with loc

## Objective

Learn how to select rows using loc.

## Syntax

df.loc[row_label]

## Examples

df.loc[0]

Select row with label 0.

df.loc[1:3]

Select rows with labels 1 through 3.

## Important

loc uses labels.

The ending value in a loc slice is included.