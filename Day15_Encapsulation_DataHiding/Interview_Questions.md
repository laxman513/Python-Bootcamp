# Day 15 Interview Questions

1. What is encapsulation in Python?
2. What is name mangling?
3. What does `__dict__` show?
4. What is a getter?
5. What is a setter?
6. What does `@property` do?
7. Why is `employee.salary` used instead of `employee.salary()`?
8. How do you make a property read-only?
9. What is a calculated property?
10. Why is `strip()` useful for validation?
11. What happens when a child overrides a property?
12. Does an overridden child property automatically inherit the parent's setter?
13. What are `fget` and `fset`?
14. How can a child reuse a parent's setter?
15. What does `super()` do?
16. Why should a child avoid `self.__balance` when `__balance` belongs to the parent?
17. Explain the BankAccount/SavingsAccount mini-project.
18. Why is `account_number` read-only?
19. Why is `interest` a calculated property?
20. Explain how validation protects the BankAccount object.
