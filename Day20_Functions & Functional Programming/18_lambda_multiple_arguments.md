# Program 18 — Lambda with Multiple Arguments

## Objective
Learn how lambda functions can accept multiple parameters.

## Example

add = lambda a, b: a + b

## Expected Output

30

## Key Point

A lambda can have multiple arguments but only one expression.